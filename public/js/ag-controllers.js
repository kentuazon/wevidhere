var WeVidHereConfig = angular.module('WeVidHere', []);

WeVidHereConfig.controller(
	'WeVidHereConfig',
	[
		'$scope', //the object we attach verything to
		'$http',   // imported Ajax object, injected into out controller
		function ($scope, $http) {
			
		}
	]



);